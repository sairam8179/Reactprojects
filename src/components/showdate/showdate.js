


const ShowDate=(props)=>{
    return(
        <>
        <p>
            {props.greeting} user ,{props.day}
        </p>
        
        </>
    )
}


export default ShowDate

{/* <ShowDate day={getDay()} greeting={getGreeting(hour)} /> for app.js*/}